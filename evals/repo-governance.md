# repo-governance

## 应触发

- 帮这个新项目写一份 AGENTS.md。
- `docs/current.md` 好像和代码对不上了，查一下哪些文档过期了。
- Audit this repo's agent context docs for drift.

## 不应触发

- 这个模块应该放 `packages/` 还是 `apps/`？（代码组织，不是上下文治理）
- 帮我写 README 的安装章节。
